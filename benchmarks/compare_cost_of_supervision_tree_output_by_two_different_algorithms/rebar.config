{erl_opts, [no_debug_info]}.

{deps, [bean]}.

{escript_incl_apps,
 [compare_cost_of_supervision_tree_output_by_two_different_algorithms]}.

{escript_main_app, compare_cost_of_supervision_tree_output_by_two_different_algorithms}.

{escript_name, compare_cost_of_supervision_tree_output_by_two_different_algorithms}.

{escript_emu_args, "%%! +sbtu +A1\n"}.

%% the plugin itself
{project_plugins, [rebar3_proper]}.

%% The PropEr dependency is required to compile the test cases
%% and will be used to run the tests as well.
{profiles,
 [{test,
   [{deps,
     %% hex
     [{proper, "1.4.0"}]},
    {erl_opts, [debug_info]}]}]}.
