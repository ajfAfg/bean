{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [time_required_to_restart]}.
{escript_main_app, time_required_to_restart}.
{escript_name, time_required_to_restart}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
